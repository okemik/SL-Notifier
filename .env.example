# Telegram credentials (required)
TELEGRAM_BOT_TOKEN=123456789:replace_with_your_bot_token
TELEGRAM_CHAT_ID=123456789

# Polling starts immediately, then repeats after each check.
# Minimum 60000 ms, including authenticated manual requests.
CHECK_INTERVAL_MS=60000
TRANSPORT_MODE=METRO
LINES=17,18,19
FUTURE=false

# Display and translation. Only Swedish-to-English automatic translation is supported.
TZ=Europe/Stockholm
PREFERRED_LANG=sv
TRANSLATE_ENABLED=true
# Translation backend. "google" (default) = built-in free, key-less chain: Google's
# dict-chrome-ex endpoint with an automatic MyMemory fallback (no config needed).
# Optional: set TRANSLATE_EMAIL to raise MyMemory's free daily character quota.
# Alternatively self-host LibreTranslate (no API key/billing):
#   docker run -p 5000:5000 libretranslate/libretranslate:1.11
#   TRANSLATE_BACKEND=libre
#   TRANSLATE_ENDPOINT=http://localhost:5000   (optional; defaults to localhost:5000)
TRANSLATE_BACKEND=google
# Optional: raises the free MyMemory daily character quota when Google is blocked.
# TRANSLATE_EMAIL=you@example.com

# State is for one Telegram destination. Docker Compose overrides this path.
STATE_DB=state.db
PRUNE_DAYS=14

PORT=3000
# Optional. Empty disables /check. Use a long random secret; send it in the Authorization header.
CHECK_API_KEY=
